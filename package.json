{
  "name": "parse_photo",
  "private": true,
  "version": "2.0.0",
  "description": "Nest TypeScript starter repository",
  "license": "MIT",
  "scripts": {
    "start": "npm run prestart --prefix backend_node && npm run start:dev --prefix backend_node",
    "start:inspect": "npm run start:inspect --prefix backend_node",
    "start:front": "npm run start --prefix frontend",
    "frontend:build": "npm run build --prefix frontend",
    "backend:build": "npm run build --prefix backend_node"
  },
  "dependencies": {},
  "devDependencies": {}
}
